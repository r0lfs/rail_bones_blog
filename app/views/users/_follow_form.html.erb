<% if @user.id != current_user.id %>
  <div id="follow_form">
    <% if current_user.following.include?(@user) %>
   	  <%= render 'unfollow' %>
  	<% else %>
    	<%= render 'follow' %>
  	<% end %>
  </div>
<% end %>