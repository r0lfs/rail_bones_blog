<h1>Edit your posts and comments by clicking on the text!</h1>
<div class="users">
	<%= render @user %>
</div>
<div class="posts">
	<% @posts.reverse.each do |post| %>
		<%= render post %>
	<% end %>
</div>
